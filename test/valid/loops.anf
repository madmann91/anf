mod loops {
    def print(i: i32) = {}
    def println(i: i32) = {}
    def range(body: i32 => ())(a: i32, b: i32) = {}

    def main() = {
        for (i <- range(0, 5)) {
            print(i)
            continue()
        }
        for (i <- range(1, 7)) print(i)
        while (false) println(42)
        while (true) {
            break()
        }
    }
}
